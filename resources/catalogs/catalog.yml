# Unity Catalog: catalog cicd_with_vibe and schema results
# Deployed with: databricks bundle deploy -t <target> --profile <profile>

resources:
  catalogs:
    cicd_with_vibe:
      name: cicd_with_vibe
      comment: Catalog for cicd_with_vibe bundle

  schemas:
    results:
      name: results
      catalog_name: ${resources.catalogs.cicd_with_vibe.name}
      comment: Schema for results data
